simpleArray = [1, 2, 3]
simpleArray[1] // returns 2 from the array

typeof 123/12 //gives "number" as type
typeof "things in quotes" //returns "string"
typeof undefined //returns undefined

hour = 10
if (hour < 18) {
    value = true
    } else {
    value = false
    };
    // returns true since hour is set to 10
